{
  "format_version": 1,
  "name": "Security Log Cleared Then Another Log Cleared",
  "description": "The security audit log is cleared (1102) and an event log file is then cleared (104, recorded in the System channel) — more than one log wiped in sequence, which a bulk clear produces and routine administration almost never does. Matching is directional: this rule finds the security log going first, so copy it with the two IDs swapped to catch the other order.",
  "relation_type": "correlation",
  "algorithm": "sequence",
  "sequence": ["1102", "104"],
  "labels": ["then clears another log"]
}
