{
  "format_version": 1,
  "name": "Password Reset Then Added To Group",
  "description": "An account password is reset by an administrator (4724) and a security-enabled local group then gains a member (4732) — taking over an existing account and privileging it rather than creating one. Helpdesk work produces the same pair, and the events are paired by host and time order, not by account.",
  "relation_type": "correlation",
  "algorithm": "sequence",
  "sequence": ["4724", "4732"],
  "labels": ["then added to group"]
}
